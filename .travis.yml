language: cpp
sudo: required
dist: trusty
compiler:
  - gcc
ruby: 2.2.0
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libsdl2-dev libsdl2-image-dev
before_script:
  - gem install magica
script:
  - magica run

